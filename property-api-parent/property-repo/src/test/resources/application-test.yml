info:
  app:
    name: property-repo
    description: Property Repo
    version: 1.0.0


spring:
  application:
    name: property-api
  jpa:
    hibernate:
      ddl-auto: validate
      naming_strategy: org.hibernate.cfg.ImprovedNamingStrategy
    show-sql: true

logging:
  config: classpath:logback-test.xml
